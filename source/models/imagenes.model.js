import db from "../config/db.js";

//id_publicacion, ruta_url, id_licencia, marca_agua_texto
export const registrarImagenModel = async (params) => {
    
}

export const listarImagenesPorPublicacion = async (params) => {
    
}