import db from '../config/db.js'

export const crearModeradorModel = async()=>{
    try {
        const query = ``;
    
    } catch (error) {
        console.log("Error en crearModeradorModel", error);
        throw error;
    }
}

export const quitarModeradorModel = async()=>{
    try {
        const query = ``;
    
    } catch (error) {
        console.log("Error en crearModeradorModel", error);
        throw error;
    }
}